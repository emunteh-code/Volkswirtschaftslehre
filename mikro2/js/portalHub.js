/** Live-portal hub slug only; no curated manifest bridge (mikro2 has no `contentManifest.js`). */
import { mountLivePortalBridge } from '../../assets/js/live-portal-bridge.js';

mountLivePortalBridge('mikro2');
